server {
    listen       80;
    listen       [::]:80;
    server_name  <ReplaceMe: server name>;

    # Increase the maximum allowed upload size
    client_max_body_size 10G;

    # Serve static assets from the public folder
    location /_next/static {
        alias <ReplaceMe: app cwd>/.next/static;
        expires 1y;
        access_log off;
    }

    location /static {
        alias <ReplaceMe: app cwd>/public;
        expires 1y;
        access_log off;
    }

    # Proxy all other requests to the Next.js app
    location / {
        proxy_pass http://localhost:<ReplaceMe: port number>;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}